import { spliceItem, omitObj, concatUniqStrArrays, deduplicate, splitDeepPath, joinDeepPath } from 'squidlet-lib';
export declare const stdLib: {
    spliceItem: typeof spliceItem;
    concatUniqStrArrays: typeof concatUniqStrArrays;
    deduplicate: typeof deduplicate;
    omitObj: typeof omitObj;
    splitDeepPath: typeof splitDeepPath;
    joinDeepPath: typeof joinDeepPath;
};
